import cv2
from cv2 import cvtColor
import numpy as np
goruntu = cv2.imread('standartresim.jpeg',0)
xyukseklik, genislik = goruntu.shape
print(goruntu.shape)
canny = cv2.Canny(goruntu,10,5)
cv2.imshow('Standart Resim',goruntu)
cv2.imshow('Canny',canny)
cv2.waitKey(0)
 
